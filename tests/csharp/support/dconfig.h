/**
 * Stub header for the test harness.
 *
 * interrogate's generated pass-1 supplemental includes "dconfig.h" (a panda3d
 * header that provides Configure / ConfigureFn macros), but nothing in the
 * supplemental actually uses them.  We provide this empty stub so the
 * generated code compiles without the full panda3d dtool headers.
 */
#pragma once
